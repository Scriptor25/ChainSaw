def vec3;
def point3 = vec3;
def color = vec3;
def ray;
def interval;

def hit_record;
def hittable;
def sphere;
def hittable_list;

@random::flt64 = rand() / (RAND_MAX + 1.0);
@random(min: flt64, max: flt64): flt64 = min + (max - min) * random();
